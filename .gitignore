# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/

# The .xcodeproj is committed for one-command setup. It's generated from
# project.yml via xcodegen — see README. Per-user state inside the project
# is still ignored via the entries above.

# Swift Package Manager
.swiftpm/
Package.resolved

# Node (worker/)
node_modules/

# Cloudflare Workers
.wrangler/

# Claude Code local settings
.claude/

# macOS
.DS_Store

# Secrets
.env
.env.local
Secrets.swift

# Demo source media — kept local only; the optimized GIF lives in docs/demo.gif
/demo.gif
/demo.webm
